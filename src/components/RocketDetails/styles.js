import { StyleSheet } from 'react-native'

export const styles = StyleSheet.create({
  container: {
    borderWidth: 1,
    borderColor: '#222',
    borderRadius: 2
  },
  row: {
    backgroundColor: '#fff',
    borderRadius: 2
  },
  text: {
    color: '#222',
    fontSize: 22
  }
})
